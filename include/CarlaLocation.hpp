#include <map>
#include <array>
#include <vector>

std::map<std::string, std::map<int, std::vector<float>>> truckLocations = {
    {"Town04_Opt", {
        {0, {-270.990f, 30.0f, 2.0f}},
        {1, {-300.990f, 30.0f, 2.0f}},
        {2, {-330.990f, 30.0f, 2.0f}},
        {100, {-90.990f, 31.5f, 11.0f}} // 예시 위치: 기존 트럭들보다 훨씬 먼 x좌표
    }},
    {"IHP", {
        {0, {-1139.0f, 4.5f, 1.0f}},
        {1, {-1169.0f, 4.5f, 1.0f}},
        {2, {-1199.0f, 4.5f, 1.0f}}
    }},
    {"k-track", {
        {0, {-75.6f,  50.75f,  2.0f}},
        {1, {-95.6f, 50.75f, 2.0f}},
        {2, {-115.6f, 50.75f, 2.0f}},
        {3, {-135.6f, 50.75f, 2.0f}},
        {4, {-155.6f, 50.75f, 2.0f}},
        // --- 긴급 상황 트럭 추가 ---
        // 'k-track' 맵에 100번 트럭을 위한 위치 설정
        // 이 위치는 기존 트럭들보다 훨씬 멀리 떨어져 있어야 합니다.
        // x, y, z 값을 실제 트랙의 긴급 상황 위치에 맞게 조절하세요.
        // z값은 바닥으로부터의 높이이며, 트럭이 바닥에 잘 놓이도록 조절해야 합니다.
        {100, {200.0f, 50.75f, 2.0f}} // 예시 위치: 기존 트럭들보다 훨씬 먼 x좌표
    }

    }

};

std::map<std::string, std::map<int, std::vector<float>>> truckRotations = {
    {"Town04_Opt", {
        {0, {0.0f, 0.0f, 0.0f}},
        {1, {0.0f, 0.22f, 0.0f}},
        {2, {0.0f, 0.22f, 0.0f}},
        {100, {0.0f, 90.0f, 0.0f}} // 예시 위치: 기존 트럭들보다 훨씬 먼 x좌표
    }},
    {"IHP", {
        {0, {0.0f, 0.0f, 0.0f}},
        {1, {0.0f, 0.0f, 0.0f}},
        {2, {0.0f, 0.0f, 0.0f}}
    }},
    {"k-track", {
        {0, {0.0f, 0.0f, 0.0f}},
        {1, {0.0f, 0.0f, 0.0f}},
        {2, {0.0f, 0.0f, 0.0f}},
        {3, {0.0f, 0.0f, 0.0f}},
        {4, {0.0f, 0.0f, 0.0f}},
        // --- 긴급 상황 트럭 추가 ---
        // 'k-track' 맵에 100번 트럭을 위한 회전 설정
        // 가로로 길을 막는 회전이라면 yaw 값을 90.0 또는 -90.0 등으로 조절해야 합니다.
        {100, {0.0f, 90.0f, 0.0f}} // 예시 회전: yaw 90도 (길을 가로막는 형태)
    }}
};